---
paths:
  - "packages/core/src/schemas/**"
  - "packages/core/src/frontmatter.ts"
  - "packages/core/schema/**"
  - "packages/core/scripts/**"
  - "packages/core/test/fixtures/**"
---

# Format

Code here is claimed by the format domain. Changing it means updating `domainbook/domains/format/` in the same commit, or waiving the commit with a "Skip-Docs: <reason>" trailer. Any file under that folder clears the check: the canvas, the glossary, the changelog, a feature, a decision, or a debt record.

Before you name anything here, call `explain_terms` with the words you are about to use — this context has its own, and they win over the book's. `domainbook/domains/format/index.md` holds its canvas.
