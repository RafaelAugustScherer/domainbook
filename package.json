{
  "name": "domainbook-monorepo",
  "private": true,
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=24.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --build",
    "test": "vitest run",
    "schemas": "npm run schemas --workspace @domainbook/core",
    "changeset": "changeset"
  },
  "devDependencies": {
    "@changesets/cli": "2.31.1",
    "@types/node": "^24.9.2",
    "typescript": "6.0.3",
    "vitest": "4.1.10"
  }
}
